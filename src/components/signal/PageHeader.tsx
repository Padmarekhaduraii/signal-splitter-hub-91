import type { ReactNode } from "react";
export function PageHeader({ eyebrow, title, description, action }: { eyebrow: string; title: string; description: string; action?: ReactNode }) { return <section className="sig-fade mb-8 flex flex-wrap items-end justify-between gap-4"><div><p className="eyebrow text-brand">{eyebrow}</p><h1 className="mt-2 max-w-3xl text-balance text-3xl font-semibold leading-tight text-ink md:text-4xl">{title}</h1><p className="mt-2 max-w-2xl text-pretty text-sm leading-relaxed text-ink/60">{description}</p></div>{action}</section>; }
export function Panel({ children, className = "" }: { children: ReactNode; className?: string }) { return <section className={`rounded-lg bg-panel/55 p-5 ring-1 ring-ink/5 backdrop-blur-xl ${className}`}>{children}</section>; }
