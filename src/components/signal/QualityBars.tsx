import { quality } from "./data";
export function QualityBars({ detailed = false }: { detailed?: boolean }) { return <div className="space-y-5">{quality.map((item) => <div key={item.label}><div className="flex justify-between text-sm font-medium text-ink/70"><span>{item.label}</span><span className="font-mono text-ink">{item.value}%</span></div><div className={`mt-2 overflow-hidden rounded-full bg-ink/10 ${detailed ? "h-2.5" : "h-1.5"}`}><div className={`sig-fill h-full rounded-full ${item.color}`} style={{ "--target-width": `${item.value}%` } as React.CSSProperties} /></div>{detailed && <p className="mt-2 text-xs text-ink/50">{item.value >= 90 ? "Ready to publish with high confidence." : "Review highlighted opportunities before publishing."}</p>}</div>)}</div>; }
